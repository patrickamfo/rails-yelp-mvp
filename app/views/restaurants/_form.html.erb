<%= simple_form_for(@restaurant) do |f| %>

<div>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
</div>

<div>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
</div>
